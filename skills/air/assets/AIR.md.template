# AIR

<!-- air:air-schema 1 -->

How this repository talks to AIR Kit. Later `/air` commands read this file and `PARTNER.md` before acting.

## SDK

- Package: `@mocanetwork/airkit`
- Version: {{sdkVersion}}
- BUILD_ENV: {{buildEnv}}
- credentialNetwork: {{credentialNetwork}}

## Auth

- Model: {{authModel}}
- Partner JWT header: `x-partner-auth`
- Issuer backend headers: `x-api-key` / `x-admin-api-key`

## Issuance

- Path: {{issuancePath}}
- Browser never calls `/available-vc` or `/issue-vc`

## Verification

- Path: {{verificationPath}}

## Paymaster / gas

- {{paymaster}}

## Environment split

Browser-safe (`NEXT_PUBLIC_*` only):

- `NEXT_PUBLIC_PARTNER_ID`
- `NEXT_PUBLIC_ISSUER_DID`
- `NEXT_PUBLIC_ISSUE_PROGRAM_ID`
- `NEXT_PUBLIC_VERIFY_PROGRAM_ID`
- `NEXT_PUBLIC_BUILD_ENV`

Issuer backend (`apps/backend/.env`, never `NEXT_PUBLIC_*`):

- `NODE_ENV`
- `DATABASE_URL`
- `ISSUER_ORIGIN`
- `SEED`
- `PARTNER_ID`
- `PARTNER_PRIVATE_KEY_KID`
- `PARTNER_PRIVATE_KEY_DER`
- `SD_JWT_JWKS`
- `API_KEY`
- `ADMIN_API_KEY`

Web server-only (never `NEXT_PUBLIC_*`):

- `PARTNER_PRIVATE_KEY_KID`
- `PARTNER_PRIVATE_KEY_DER`
- `SD_JWT_JWKS`

## Anti-references

- Do not put `SEED` or `PARTNER_PRIVATE_KEY_DER` in `NEXT_PUBLIC_*`
- Do not sign Partner JWTs in the browser
- Do not reuse one JWT across users or across issue + verify without the right scope
- Do not assume Testnet credentials exist on mainnet
- Do not skip JWKS because login already works
- Do not call Privy directly for agentic wallets
